#include <stdio.h>
int main() 
{
    int N;
    printf("Ingrese el valor de N: ");
    scanf("%d", &N);
    int i = 1;
    printf("\n");
    i = 1;
    do {
        printf("%d ", i);
        i++;
    } while (i <= N);
    printf("\n");
    for (i = 1; i <= N; i++)
    getch();
    return 0;
}
